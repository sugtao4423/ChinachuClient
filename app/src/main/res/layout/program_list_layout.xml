<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="@dimen/paddingProgramItemHorizontal"
        android:paddingVertical="@dimen/paddingProgramItemVertical">

        <TextView
            android:id="@+id/programTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/titleText"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/programDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/paddingProgramItemSpace"
            android:textColor="@color/dateText"
            android:textSize="12sp" />

    </LinearLayout>
</layout>
